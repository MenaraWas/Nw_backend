# Database connection string (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/nw_backend?schema=public"

# JWT Secret
JWT_SECRET="your-super-secret-jwt-key"

# Port (Railway will automatically inject PORT)
PORT=3000

